{
	"name": "p4-ai-service",
	"version": "1.0.0",
	"description": "P4 AI bot service — connects to the game via WebSocket and plays using minimax",
	"type": "module",
	"main": "dist/index.js",
	"scripts": {
		"prestart": "tsc",
		"start": "node dist/index.js",
		"build": "tsc",
		"dev": "tsx watch src/index.ts"
	},
	"dependencies": {
		"dotenv": "^16.4.5",
		"socket.io-client": "^4.8.1"
	},
	"devDependencies": {
		"@types/node": "^20.19.0",
		"tsx": "^4.21.0",
		"typescript": "^5.5.2"
	}
}
